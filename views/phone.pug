extends layout

block content
  - let create = `dispatch(\"/phone/create\")`
  button.btn(class="btn-primary mb-2" onclick=create) Add a Phone
    h1= title

    
    div(class="container-fluid")
        div(class="row")
            -
                let phone = results
            each v in phone
                - let destination = `dispatch(\"/phone/update?id=${v._id}\")`
                - let detail = `dispatch(\"/phone/detail?id=${v._id}\")`
                - let del = `dispatch(\"/phone/delete?id=${v._id}\")`

                div(class="row-sm-2" class='m-1') Phone #{v.name}(#{v.brand}) is #{v.cost}
                    button.btn(class="btn-success ml-1", onclick=detail) Detail
                    button.btn(class="btn-primary ml-1", onclick=destination) Update
                    button.btn(class="btn-danger ml-1", onclick=del) Delete
                
            script.
                function dispatch(path){
                    window.location.href = path
                }